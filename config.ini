[OCR]
engine = manga-ocr
language = ja

[TRANSLATION]
engine = opus-mt
source_lang = ja
target_lang = en

[ANKI]
server = http://127.0.0.1:8765
deck = Japanese
model = Basic
tags = OCRPeeker

# Hotkeys are configured in your Hyprland config, not here.
# Add to ~/.config/hypr/hyprland.conf:
#   bind = CTRL SHIFT, S, exec, ocrpeeker-client capture
#   bind = CTRL SHIFT, Q, exec, ocrpeeker-client quit
